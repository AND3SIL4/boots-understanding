@AGENTS.md

<!--
  CLAUDE.md es solo un import: todo el contexto real vive en AGENTS.md
  (formato abierto, también lo lee OpenCode de forma nativa). Se hace así
  para no mantener el mismo contenido en dos archivos por separado.
  Agrega aquí abajo únicamente instrucciones específicas de Claude Code,
  si alguna vez hacen falta.
-->
